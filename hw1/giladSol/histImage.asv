function h = histImage(img)



%DELETE
img = readImage('barbarasmall.tif');
%DELETE

h = zeros(1,256);
for i=0:255
    h(i+1) = length( find(img == i) );
end



h = zeros(1, 256);
for i=0:255
    count = size(find(img==i));
    h(i+1) = count(1);
end

a = find(img == 249);
b = size(a);
c = length(a);
